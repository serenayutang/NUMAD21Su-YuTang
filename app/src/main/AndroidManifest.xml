<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="neu.madcourse.numad21su_yutang">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_serena"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_serena_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SuYuTang">
        <activity android:name=".SixBtns" />
        <activity android:name=".LinkCollectorActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".AtServiceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>